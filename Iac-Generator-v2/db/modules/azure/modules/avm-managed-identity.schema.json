{
  "typeName": "Microsoft.ManagedIdentity/userAssignedIdentities",
  "provider": "azure",
  "description": "Azure User Assigned Managed Identity (AVM)",
  "supportedPlatforms": [
    "terraform"
  ],
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of the user-assigned managed identity."
    },
    "resource_group_name": {
      "type": "string",
      "description": "Name of the existing Azure resource group."
    },
    "location": {
      "type": "string",
      "description": "Azure region (e.g., eastus, westeurope)."
    },
    "tags": {
      "type": "object",
      "description": "Resource tags as JSON object."
    },
    "enable_telemetry": {
      "type": "boolean",
      "description": "Enable AVM telemetry."
    }
  },
  "required": [
    "name",
    "resource_group_name",
    "location"
  ]
}